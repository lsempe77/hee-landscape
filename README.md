# HEE evidence map — interactive
Shinylive (WebR) build of the Shiny app in `projects/hee/analysis/shiny` of the HSF repo. First load downloads the R runtime and packages (~1-2 min); later visits are cached.
