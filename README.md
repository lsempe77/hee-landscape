# HEE evidence map — interactive

Pure static HTML/JS build (no server, no WebR). Data exported by R/10-build-webapp-data.R from the Shiny bundle; site source lives in webapp/site of the HSF analysis repo. Publish: copy webapp/site -> docs/, push.
